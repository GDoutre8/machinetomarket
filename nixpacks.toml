[phases.setup]
aptPkgs = [
  "ffmpeg",
  "fonts-dejavu-core",
  "fonts-liberation",
  "libheif1",
]

[phases.build]
cmds = [
  "python -m playwright install --with-deps chromium",
  "echo 'Chromium installed for Playwright card generation'",
]
