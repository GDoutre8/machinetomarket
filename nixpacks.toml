[phases.setup]
aptPkgs = [
  "ffmpeg",
  "fonts-dejavu-core",
  "fonts-liberation",
  "fonts-montserrat",
  "libheif1",
]

[phases.build]
cmds = [
  "python -m playwright install --with-deps chromium",
  "echo 'Chromium installed for Playwright spec sheet and card rendering'",
]
